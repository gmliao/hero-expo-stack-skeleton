// Re-export Tag and request/response types from backend api types (synced with shared/types/api.ts).
export type {
  Tag,
  CreateTagRequest,
  UpdateTagRequest,
  ListTagsResponse,
} from '../../types/api'
